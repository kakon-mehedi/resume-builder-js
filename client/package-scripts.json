{
  "scripts": {
    "setup": "npm run install-all && echo 'Setup complete! Run npm run dev to start.'",
    "dev:full": "concurrently \"npm run server\" \"npm run client\" --names \"SERVER,CLIENT\" --prefix name --kill-others",
    "test:all": "npm test && cd client && npm test",
    "lint": "eslint . --ext .js,.jsx",
    "format": "prettier --write \"**/*.{js,jsx,json,md}\"",
    "clean": "rm -rf node_modules client/node_modules client/build",
    "reset": "npm run clean && npm run install-all"
  }
}